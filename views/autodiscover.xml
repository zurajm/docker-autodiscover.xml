<?xml version="1.0" encoding="utf-8" ?>
<Autodiscover xmlns="http://schemas.microsoft.com/exchange/autodiscover/responseschema/2006">

	{% if hasSync %}<Response xmlns="http://schemas.microsoft.com/exchange/autodiscover/mobilesync/responseschema/2006">

        <User>
            <DisplayName>{{email}}</DisplayName>
            <EMailAddress>{{email}}</EMailAddress>
        </User>

        <Action>
            <Settings>
                <Server>
                    <Type>MobileSync</Type>
                    <Url>{{sync}}</Url>
                    <Name>{{sync}}</Name>
                </Server>
            </Settings>
        </Action>

    </Response>
	
	{% endif %}><Response xmlns="{{schema}}">

		<User>
			<DisplayName>{{email}}</DisplayName>
            <EMailAddress>{{email}}</EMailAddress>
		</User>

		<Account>
			<AccountType>email</AccountType>
			<Action>settings</Action>

			<Protocol>
				<Type>IMAP</Type>
				<TTL>1</TTL>

				<Server>{{imap.host}}</Server>
				<Port>{{imap.port}}</Port>

				<LoginName>{{email}}</LoginName>

				<DomainRequired>on</DomainRequired>
				<DomainName>{{domain}}</DomainName>

				<SPA>off</SPA>
				<AuthRequired>on</AuthRequired>
				{% if imap.isStartTls %}<Encryption>tls</Encryption>{% else %}<SSL>on</SSL>{% endif %}>
			</Protocol>
		</Account>

		<Account>
			<AccountType>email</AccountType>
			<Action>settings</Action>

			<Protocol>
				<Type>SMTP</Type>
				<TTL>1</TTL>

				<Server>{{smtp.host}}</Server>
				<Port>{{smtp.port}}</Port>

				<LoginName>{{email}}</LoginName>

				<DomainRequired>on</DomainRequired>
				<DomainName>{{domain}}</DomainName>

				<SPA>off</SPA>
				<AuthRequired>on</AuthRequired>
				{% if smtp.isStartTls %}<Encryption>tls</Encryption>{% else %}<SSL>on</SSL>{% endif %}>
			</Protocol>
		</Account>

	</Response>

</Autodiscover>
